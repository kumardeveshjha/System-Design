Prototype Design Pattern is an important design Pattern when we have to update the state of the application in the nest step 
This ensures the easier object copy
This also avoids from inheritance 
This provides flxible copy options 
 ## Copy()(Shallowcopy) and Deepcopy()
This also ensures the fast object copy rather than using the iteration it makes it faster copy 
